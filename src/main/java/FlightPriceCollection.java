public class FlightPriceCollection {

    public static void main(String[] args) {





/*
        //input
        JTextField departureAirportField = new JTextField(3);
        JTextField arrivalAirportField = new JTextField(3);
        JTextField departDateField = new JTextField(10);
        JTextField returnDateField = new JTextField(10);
        Object[] popupWindow = {
                "departure airport code:", departureAirportField,
                "arrival airport code:", arrivalAirportField,
                "departure date(YYYY-MM-DD)", departDateField,
                "return date(YYYY-MM-DD)", returnDateField
        };
        if (JOptionPane.showConfirmDialog(null, popupWindow,
                "Flight Search", JOptionPane.OK_CANCEL_OPTION) != JOptionPane.OK_OPTION)
            return new String[]{};
        else {
            if (departureAirportField.getText().equals(""))
                departureAirportField.setText("foc");
            if (arrivalAirportField.getText().equals(""))
                departureAirportField.setText("sha");
            if (departDateField.getText().equals(""))
                departDateField.setText("2018-11-20");
            return new String[]{departureAirportField.getText(), arrivalAirportField.getText(),
                    departDateField.getText(), returnDateField.getText().trim()};
        }
        CompletableFuture<String[]> completableFuture = CompletableFuture.supplyAsync(() -> {
        });
        */
    }
}
